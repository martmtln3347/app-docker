# Example MySQL configuration (replace with your local/test values)
MYSQL_HOST=localhost
MYSQL_PORT=xxxx
MYSQL_DATABASE=example_db
MYSQL_USER=example_user
MYSQL_PASSWORD=example_password

# Server
PORT=3000
# Use a short, non-sensitive secret for local/dev only
JWT_SECRET=change_this_to_a_secret

# MongoDB (example local URI)
MONGO_URL=mongodb://localhost:27017/example_db
